@model int
@if (Model > 0)
{
    <span class="absolute -right-0.5 -top-0.5 z-10 flex h-5 min-w-[1.25rem] items-center justify-center rounded-full bg-gradient-to-r from-violet-600 to-fuchsia-600 px-1 text-[10px] font-bold text-white shadow-lg shadow-violet-600/40">@Model</span>
}
